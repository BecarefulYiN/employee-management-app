const theme = createTheme({
    palette: {
      primary: {
        main: '#ffffff', // specify your primary color
      },
      secondary: {
        main: 'pink', // specify your secondary color
      },
    },
  });